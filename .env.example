# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Geoapify Places API key — https://myprojects.geoapify.com/
# Used by the geoapify provider (food + fitness). If unset, that provider is
# skipped with a warning and the others (OSM, Overture) still run.
GEOAPIFY_KEY=
